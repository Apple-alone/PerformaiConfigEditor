<Window x:Class="Performai_Config_Editor.CustomMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="提示" 
        Height="250" 
        Width="400"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">
    
    <Window.Resources>
        <Style x:Key="MessageBoxButton" TargetType="Button">
            <Setter Property="Background" Value="#FF2C3E50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FFBDC3C7"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF3498DB"/>
                                <Setter Property="BorderBrush" Value="#FF2980B9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF1A5276"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <!-- 阴影效果 -->
    <Border Margin="10" Background="Transparent">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="0" BlurRadius="20" Opacity="0.3"/>
        </Border.Effect>
        
        <!-- 主窗口 -->
        <Border Background="White" CornerRadius="8" BorderThickness="1" BorderBrush="#FFE0E0E0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- 标题栏 -->
                <Border Grid.Row="0" Background="#FF2C3E50" CornerRadius="8,8,0,0" Height="45">
                    <Grid>
                        <TextBlock x:Name="TitleText" Text="提示" 
                                  Foreground="White" 
                                  FontSize="16" 
                                  FontWeight="Bold"
                                  VerticalAlignment="Center"
                                  Margin="15,0"/>
                        <Button x:Name="CloseButton" 
                               Content="✕" 
                               Foreground="White"
                               Background="Transparent"
                               BorderThickness="0"
                               FontSize="14"
                               HorizontalAlignment="Right"
                               Width="45" Height="45"
                               Click="CloseButton_Click">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="Transparent">
                                        <ContentPresenter HorizontalAlignment="Center" 
                                                          VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Grid>
                </Border>
                
                <!-- 内容区域 -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="15">
                    <StackPanel>
                        <!-- 图标和消息 -->
                        <StackPanel Orientation="Horizontal" Margin="0,10,0,15">
                            <Border x:Name="IconBorder" 
                                   Width="40" 
                                   Height="40" 
                                   CornerRadius="20"
                                   Margin="0,0,15,0">
                                <TextBlock x:Name="IconText" 
                                          FontSize="20"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock x:Name="MessageText" 
                                      TextWrapping="Wrap"
                                      VerticalAlignment="Center"
                                      FontSize="14"
                                      Foreground="#FF333333"/>
                        </StackPanel>
                        
                        <!-- 额外信息 -->
                        <TextBlock x:Name="DetailText" 
                                  TextWrapping="Wrap"
                                  FontSize="12"
                                  Foreground="#FF666666"
                                  Margin="55,0,0,10"
                                  Visibility="Collapsed"/>
                    </StackPanel>
                </ScrollViewer>
                
                <!-- 按钮区域 -->
                <Border Grid.Row="2" Background="#FFF8F9F9" CornerRadius="0,0,8,8" 
                       BorderThickness="0,1,0,0" BorderBrush="#FFE0E0E0">
                    <StackPanel x:Name="ButtonPanel" 
                               Orientation="Horizontal" 
                               HorizontalAlignment="Right"
                               Margin="15,10">
                        <!-- 按钮将在代码中动态添加 -->
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Border>
</Window>